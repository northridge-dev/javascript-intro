// 1. Create a variable called `emptyString` and assign it an empty string

// 2. Create a variable called `text` and assign it a string that is between 10 and
//    20 characters long

// 3. Create a variable called `upperCase` and assign it a string that only
//    contains uppercase letters

// 4. Create a variable called `sentence` and assign it a string that has
//    exactly 10 words (space-separated).
