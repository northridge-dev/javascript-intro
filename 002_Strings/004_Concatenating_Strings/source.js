/*
 * Create a variable called `greeting`.
 * Set it equal to "Hello".
 */

/*
 * Create a variable called `name`.
 * Set it equal to a string that contains a name.
 */

/*
 * Create a variable called `welcomeMessage`.
 * Concatenate `greeting` and `name` so that
 * `welcomeMessage` equals "Hello, [name]!"
 */
