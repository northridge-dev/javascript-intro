let name = "Pablito";
let adjective = "pugnacious";
let animal = "platypus";
let location = "playa";

/*
 * Use the `concat` method to concatenate the `name`, `adjective`,
 * `animal`, and `location` variables so that the result is:
 * "Pablito the pugnacious platypus struts across the playa."
 * Store the result in a variable called `concatString`.
 */
