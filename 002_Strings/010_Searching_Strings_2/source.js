// Add the example code from the instructions here

/**
 * TESTS!
 * Replace `undefined` with the index of the substring (word or character)
 * in the string stored as the variable `beginning`.
 *
 * For example:
 * let index_of_c = 2;
 *
 * (If you're feeling clever, use `indexOf` instead of counting characters
 * yourself!)
 */

let beginning = "Once upon a time...";

let index_of_Once = undefined;
let index_of_time = undefined;
let index_of_p = undefined;
let index_of_first_space = undefined;
