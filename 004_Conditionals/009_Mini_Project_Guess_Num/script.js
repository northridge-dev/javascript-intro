function checkGuess(guess, upperBound, lowerBound, secretNumber) {
  // Write your code here
}

// ------ DO NOT DELETE THIS CODE OR CHANGE VARIABLE NAMES ------
const lowerBound = 1; // change value to set the lower bound (inclusive)
const upperBound = 10; // change value to set the upper bound (inclusive)
