function multiply(list) {
  let product = 1;
  for (let number of list) {
    product = product * number;
  }

  return product;
}

function add(list) {
  // write your code here; use a for...of loop
}
